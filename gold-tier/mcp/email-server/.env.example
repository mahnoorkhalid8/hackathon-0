# MCP Email Server - Environment Configuration

# ============================================================================
# Server Configuration
# ============================================================================

# Server name
SERVER_NAME=silver-tier-email-server

# Server version
SERVER_VERSION=1.0.0

# Environment (development, production)
NODE_ENV=development

# ============================================================================
# Email Configuration (SMTP)
# ============================================================================

# SMTP server host
# Examples:
#   Gmail: smtp.gmail.com
#   Outlook: smtp-mail.outlook.com
#   SendGrid: smtp.sendgrid.net
EMAIL_HOST=smtp.gmail.com

# SMTP server port
# Common ports:
#   587 - TLS (recommended)
#   465 - SSL
#   25  - Unencrypted (not recommended)
EMAIL_PORT=587

# Use secure connection (true for port 465, false for 587)
EMAIL_SECURE=false

# SMTP authentication username (usually your email address)
EMAIL_USER=your-email@gmail.com

# SMTP authentication password
# For Gmail: Use App Password (not your regular password)
# Generate at: https://myaccount.google.com/apppasswords
EMAIL_PASSWORD=your-app-password-here

# Default sender email address (optional, defaults to EMAIL_USER)
EMAIL_FROM=your-email@gmail.com

# ============================================================================
# Logging Configuration
# ============================================================================

# Log level (error, warn, info, debug)
LOG_LEVEL=info

# Log file path (relative to project root)
LOG_FILE=logs/email-server.log

# Maximum log file size (e.g., 10m, 100k)
LOG_MAX_SIZE=10m

# Maximum number of log files to keep
LOG_MAX_FILES=5

# Enable console logging (true/false)
LOG_CONSOLE=true

# ============================================================================
# Security Configuration
# ============================================================================

# Maximum email size in bytes (default: 10MB)
MAX_EMAIL_SIZE=10485760

# Maximum number of recipients per email (to + cc + bcc)
MAX_RECIPIENTS=50

# Allowed recipient domains (comma-separated, empty = allow all)
# Example: company.com,partner.com
ALLOWED_DOMAINS=

# Blocked recipient domains (comma-separated)
# Example: spam.com,blocked.com
BLOCKED_DOMAINS=

# Rate limit: maximum emails per minute
RATE_LIMIT_PER_MINUTE=60

# ============================================================================
# MCP Configuration
# ============================================================================

# MCP transport type (stdio, http)
MCP_TRANSPORT=stdio

# MCP server port (only for http transport)
MCP_PORT=3000
